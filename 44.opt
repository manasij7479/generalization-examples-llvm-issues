; https://github.com/llvm/llvm-project/issues/60799
; Optimal

%arg:i32 = var
%eq = eq %arg, 0
%cttz = cttz  %arg
%shl = shlnuw 1, %cttz
%sel = select %eq, 0, %shl
infer %sel
%neg = sub 0, %arg
%and = and %arg, %neg
result %and

